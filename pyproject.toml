[project]
name = "query-as-natural-language"
version = "0.1.0"
description = "Generate SQL queries from natural language prompts using Gemini"
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "google-generativeai>=0.3.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
